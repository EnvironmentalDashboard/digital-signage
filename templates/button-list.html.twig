<ul class="list-group list-group-flush">
    {% for button in buttons %}
        <li class="list-group-item"><img style="width: 100%" draggable="true" ondragstart="dragstart_handler(event);" ondragend="dragend_handler(event);" id="button{{button.id}}" src="{{ path('index') }}uploads/{{button.image}}" alt="Button image"></li>
    {% endfor %}
</ul>
